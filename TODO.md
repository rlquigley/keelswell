# TODO

Open items only; closure history lives in the session-wrap triage
reports and this file's git history.

Last updated: 2026-08-08 (wrap, v0.7.0 slate v3 session)

1. Tag v0.7.0 after the slate v3 PR merges and push the tag (RQ by
   hand): `git tag v0.7.0 && git push origin v0.7.0`.
2. keelswell-manual: land the fork-arc findings file plus a KNOWN-GAPS
   update for v0.7.0 slate v3 (SYNC v2 fork-arc convention).
3. isi: commit the four new agent skill dirs (both tool trees) and the
   four `[agents.*]` tables in `_bmad/config.toml` by hand.
4. ffbapp: log the IAP-versus-FR-47 store-billing collision for Bayle
   Domon and the architecture stage (from the 2026-08-07 bench-gap
   review; Egeanin Tamarath now owns the store-rules half).
5. ffbapp: run the new-seat party sweep (Gill, Talmanes, Egeanin,
   Aludra) over the final PRD before the architecture stage, so the
   four surfaces with founder-only tags get bench review tags.
